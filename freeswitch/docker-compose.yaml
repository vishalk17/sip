version: '3.8'

services:
  freeswitch1:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: freeswitch1
      #  command: tail -f /dev/null  # Keeps the container running indefinitely
    restart: always
    network_mode: "host"
    cap_add:
      - SYS_NICE
      - SYS_RESOURCE
